const PaginationComponents = ({total, limit}) => {
    const noOfPags = Math.Floor(total / limit)
    return ( 
        <div className="pagination-wrapper">
            {
            
            }
        </div>
     );
}
 
export default PaginationComponents;